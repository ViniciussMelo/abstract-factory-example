import CheckboxComponent from "../../shared/base/checkboxComponent";

export default class MacOSCheckbox extends CheckboxComponent {
  check() {
    console.log('🍎 macOS checkbox checked!!');
  }
}